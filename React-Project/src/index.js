import ReactDOM from 'react-dom/client';
// importing our react dependancies (in our package.json)

import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
//jsx syntax

//tells our index.html file to create a root and makes this file the main page that renders first

